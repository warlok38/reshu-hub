import React from 'react';
import { StyledAuthLayout, LoginForm } from 'features/auth';

const Login = () => {
  return (
    <StyledAuthLayout>
      <LoginForm />
    </StyledAuthLayout>
  );
};

export default Login;
