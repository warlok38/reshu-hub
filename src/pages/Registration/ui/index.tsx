import React from 'react';
import { StyledAuthLayout, RegistrationForm } from 'features/auth';

const Registration = () => {
  return (
    <StyledAuthLayout>
      <RegistrationForm />
    </StyledAuthLayout>
  );
};

export default Registration;
